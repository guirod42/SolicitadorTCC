import React, { forwardRef } from 'react';
import {
    Container
} from "./styles";

const Task = forwardRef((props: any, ref) => {
    return (
        <Container/>
        );
});

export default Task;