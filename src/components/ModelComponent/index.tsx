import React, { forwardRef, useState } from 'react';
import {
    Container
} from "./styles";

const ModelComponent = forwardRef((props: any, ref) => {
    return (
        <Container>
            
        </Container>
        );
});

export default ModelComponent;