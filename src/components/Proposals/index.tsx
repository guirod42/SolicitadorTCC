import React, { forwardRef, useState } from 'react';
import {
    Container
} from "./styles";

const Proposals = forwardRef((props: any, ref) => {
    return (
        <Container/>
        );
});

export default Proposals;